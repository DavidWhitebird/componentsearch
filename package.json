{
  "name": "componentsearchherokubuild",
  "version": "0.0.1",
  "engines": {
    "node": "8.9.4",
    "npm": "6.1.0"
  },
  "cacheDirectories": [
    "client/node_modules",
    "auth_server/node_modules"],
  "dependencies": {
    "react-scripts": "1.1.4"
  },
  "scripts": {
    "startserver": "`/auth_server/node authorization_code/app.js`",
    "heroku-postbuild": "cd client/ && npm install && npm run build && npm start && cd ../auth-server && npm install && node authorization_code/app.js"
  }
}
