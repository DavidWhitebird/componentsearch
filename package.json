{
  "name": "componentsearchherokubuild",
  "version": "0.0.1",
  "engines": {
    "node": "8.9.4",
    "npm": "6.1.0",
    "yarn":"^1.7.0"
  },
  "cacheDirectories": [
    "client/node_modules",
    "auth-server/node_modules"],
  "scripts": {
    "preinstall": "cd auth-server && yarn install && node authorization_code/app.js",
    "start": "cd client && yarn install && yarn serve -s build",
    "heroku-postbuild": "cd auth-server && yarn install && node authorization_code/app.js"
  }
}